import React from "react";
import styling from "./Tasks.module.css";

const Tasks = () => {
    return (
        <div className={styling.title}>
            
        </div>
    );
};

export default Tasks;
